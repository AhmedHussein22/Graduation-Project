import 'package:flutter/material.dart';
import 'package:tot/ui/views/reviews/review.dart';

class TapReviews extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        Review(),
      ],
    );
  }
}